name: Frontend CI/CD

on:
  workflow_dispatch: {}
  push:
    branches: ["main"]
    paths:
      - "halisaha-fe/**"
      - ".github/workflows/fe-ci-cd.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "halisaha-fe/**"
      - ".github/workflows/fe-ci-cd.yml"

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/halisaha-fe

defaults:
  run:
    working-directory: halisaha-fe

jobs:
  build:
    name: Install & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: halisaha-fe/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

  build-and-push:
    name: Build & Push Docker Image
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./halisaha-fe
          file: ./halisaha-fe/Dockerfile
          push: true
          build-args: |
            VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha,scope=fe
          cache-to: type=gha,mode=max,scope=fe

  deploy:
    name: Deploy to EC2
    needs: build-and-push
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            mkdir -p /opt/halisaha-fe
            cat > /opt/halisaha-fe/docker-compose.yml <<'EOF'
            services:
              web:
                image: ${{ env.IMAGE_NAME }}:latest
                container_name: halisaha-web
                restart: unless-stopped
                ports:
                  - "80:80"
            EOF
            cd /opt/halisaha-fe
            docker compose pull
            docker compose up -d
            docker image prune -f
